server:
  port: 9091

basis:
  simple:
    rpc:
      simple-rpc-server-port: 9999
      max-wait-time: 5
      nacos:
        group-name: provider-group
        cluster-name: provider-cluster

spring:
  application:
    name: simple-rpc-provider

nacos:
  discovery:
    server-addr: wenpan-host:8848
